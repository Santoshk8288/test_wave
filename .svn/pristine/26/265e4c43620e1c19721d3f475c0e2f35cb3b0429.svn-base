import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AddQuesPaperComponent } from './add-ques-paper.component';

describe('AddQuesPaperComponent', () => {
    let component: AddQuesPaperComponent;
    let fixture: ComponentFixture < AddQuesPaperComponent > ;

    beforeEach(async (() => {
        TestBed.configureTestingModule({
                declarations: [AddQuesPaperComponent]
            })
            .compileComponents();
    }));

    beforeEach(() => {
        fixture = TestBed.createComponent(AddQuesPaperComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});